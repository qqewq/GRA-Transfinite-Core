from setuptools import setup, find_packages

setup(
    name="gra-transfinite-core",
    version="0.1.0",
    packages=find_packages(),
    description="GRA transfinite nullification engine",
    author="qqewq",
    python_requires=">=3.8",
)
